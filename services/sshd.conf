# Service Registry Definition: OpenSSH / Dropbear SSH Server
NAME="sshd"
PROCESS="sshd"
START="sh /data/local/server/startup/30-ssh.sh"
STOP="pkill sshd 2>/dev/null || pkill dropbear 2>/dev/null"
CHECK_PORT=8022
ENABLED=1
MAX_FAILURES=3
WINDOW_SECONDS=60
