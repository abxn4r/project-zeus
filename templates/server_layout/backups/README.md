# /data/local/server/backups/

## Purpose
Destination directory for local backup tarballs (`.tar.gz`) generated by `scripts/backup.sh`. Includes archived configurations, custom scripts, and log historical snapshots.
