# /data/local/server/bin/

## Purpose
Holds custom compiled binaries, executable helper scripts, and CLI wrappers added to the global system `$PATH`.

## Permissions & Policies
- **Permissions**: `0755` (`rwxr-xr-x`)
- **Owner**: `root:root`
- **PATH Inclusion**: Added to PATH in `00-bootstrap.sh`.
